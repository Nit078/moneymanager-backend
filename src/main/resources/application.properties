    # ===============================
    # MySQL Configuration
    # ===============================
    spring.datasource.url=jdbc:mysql://localhost:3306/moneymanager
    spring.datasource.username=nitin
    spring.datasource.password=mypassword


    # ===============================
    # Server Configuration
    # ===============================
    server.servlet.context-path=/api/v1.0


    # ===============================
    # JPA / Hibernate Configuration
    # ===============================
    spring.jpa.hibernate.ddl-auto=update
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.format_sql=true


    # ===============================
    # Email Configuration
    # ===============================
    spring.mail.host=smtp-relay.brevo.com
    spring.mail.port=587
    spring.mail.username=96aa89001@smtp-brevo.com
    spring.mail.password=D03CqncQE4VmO7dj
    spring.mail.properties.mail.smtp.auth=true
    spring.mail.properties.mail.smtp.starttls.enable=true
    spring.mail.protocol=smtp
    spring.mail.properties.mail.smtp.from=nitinsri2954@gmail.com

    # ===============================
    # JWT Configuration
    # ===============================
    jwt.secret=2b8e7c7e-4e2a-4b1a-9c2e-8f7e2d3c4b5a

    # ===============================
    # App URLs
    # ===============================
    money.manager.frontend.url=http://localhost:5173
    # Append the token from backend code, do NOT add /api/v1.0/activate again
    app.activation.url=http://localhost:8080

    # ===============================
    # Profiles
    # ===============================
    # spring.profiles.active=prod
# ===============================
# MySQL Configuration (Clever Cloud)
# ===============================
spring.datasource.url=jdbc:mysql://${MYSQL_ADDON_HOST}:${MYSQL_ADDON_PORT}/${MYSQL_ADDON_DB}
spring.datasource.username=${MYSQL_ADDON_USER}
spring.datasource.password=${MYSQL_ADDON_PASSWORD}

# ===============================
# HikariCP connection pool settings (limit connections for free plan)
# ===============================
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
